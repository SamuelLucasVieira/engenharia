
PG
CREATE TABLE IF NOT EXISTS service (
    id SERIAL PRIMARY KEY,
    natureza_chamado VARCHAR(20) NULL,
    descricao VARCHAR (600)  NOT NULL
);
ALTER TABLE service
    ADD CONSTRAINT natureza_chamado_not_null CHECK (natureza_chamado <> '');
ALTER TABLE service
    ADD CONSTRAINT descricao_not_null CHECK (descricao <> '');



mariadb
    -- Criação da tabela 'service' se não existir
CREATE TABLE IF NOT EXISTS service (
    id SERIAL PRIMARY KEY,
    natureza_chamado VARCHAR(20) NOT NULL,
    descricao VARCHAR(600) NOT NULL
);
-- Exemplo de restrição de CHECK personalizada para natureza_chamado
ALTER TABLE service
    ADD CONSTRAINT natureza_chamado_not_null CHECK (natureza_chamado <> '');

-- Exemplo de restrição de CHECK personalizada para descricao
ALTER TABLE service
    ADD CONSTRAINT descricao_not_null CHECK (descricao <> '');
